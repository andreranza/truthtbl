# expand_truth_tbl() works

    Code
      expand_truth_tbl(character(0))
    Condition
      Error in `expand_truth_tbl()`:
      ! length(fact_names) > 0 is not TRUE

# expand_truth_tbl_impl() works

    Code
      expand_truth_tbl_impl(1.5)
    Condition
      Error in `expand_truth_tbl_impl()`:
      ! is_integer(dim) is not TRUE

